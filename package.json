{
  "name": "harborline-demo-banking",
  "version": "1.0.0",
  "private": true,
  "description": "Fictional sandbox banking web app and local webhook receiver",
  "scripts": {
    "start:web": "python3 -m http.server 8000",
    "start:webhook": "node webhook-server.js",
    "check": "node --check app.js && node --check webhook-server.js",
    "check:android": "gradle -p android-app tasks --no-daemon"
  }
}
